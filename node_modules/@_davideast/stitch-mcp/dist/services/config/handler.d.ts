import type { GcloudService } from '../gcloud/spec';
import type { ResolveContextResult, ConfigService } from './spec';
export interface FileSystem {
    existsSync(path: string): boolean;
    readFileSync(path: string, encoding: BufferEncoding): string;
}
export declare class ConfigHandler implements ConfigService {
    private gcloud;
    private fs;
    constructor(gcloud: GcloudService, fs?: FileSystem);
    resolveContext(): Promise<ResolveContextResult>;
}
